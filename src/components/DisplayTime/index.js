import React, { PropTypes } from "react"

const propTypes = {}
const defaultProps = {}

const DisplayTime = () => {
  return <div>DisplayTime</div>
}

DisplayTime.propTypes = propTypes
DisplayTime.defaultProps = defaultProps

export default DisplayTime
