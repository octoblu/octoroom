import React, { PropTypes } from 'react'

const propTypes = {}
const defaultProps = {}

const SkypeInSession = () => {
  return <div>SkypeInSession</div>
}

SkypeInSession.propTypes    = propTypes
SkypeInSession.defaultProps = defaultProps

export default SkypeInSession
